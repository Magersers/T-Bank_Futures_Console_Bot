# T-Bank Futures Console Bot

Консольный Python-бот для T-Bank Invest API (бывш. Тинькофф Инвестиции), который:

- Открывает LONG и/или SHORT одновременно.
- Поддерживает отдельный аккаунт для SHORT (второй токен + второй `account_id`), чтобы торговать инструмент в обе стороны на разных счетах.
- Делит общий объем лотов на сторону максимум на **3 независимые сделки**.
  - Пример: `6` лотов в LONG = `3` сделки по `2` лота.
- Целевой выход по прибыли: **минимум +0.09% net** (после вычета комиссии 0.05%).
- Стоп-лосс: **-0.2%**, после чего включается пауза новых входов на **2 минуты**.
- Кэширует токен/портфель/figi/лимиты в `~/.tbank_futures_bot/config.json`.
- Получает данные стакана через **MarketData stream** (bidirectional stream), одним потоком без polling.

## Важно

- Это **не** гарантия дохода. На реальном рынке возможны проскальзывания, комиссии и убытки.
- Перед реальной торговлей протестируй на небольших объемах/песочнице.

## Установка

```bash
python -m venv .venv
source .venv/bin/activate
pip install t-tech-investments --index-url https://opensource.tbank.ru/api/v4/projects/238/packages/pypi/simple
```

## Запуск

```bash
python bot.py
```

При запуске бот спросит:

1. токен LONG,
2. `account_id` LONG,
3. токен SHORT (второй аккаунт),
4. `account_id` SHORT,
5. FIGI инструмента,
6. общий объем лотов LONG (0..N),
7. общий объем лотов SHORT (0..N).

## Логика

- При старте объем на сторону разбивается на максимум 3 независимые сделки.
  - `1` -> `[1]`
  - `2` -> `[1, 1]`
  - `3` -> `[1, 1, 1]`
  - `6` -> `[2, 2, 2]`
  - `7` -> `[3, 2, 2]`
- Цена берется из потокового стакана (`market_data_stream`, `depth=1`).
- При наличии свободного слота бот открывает недостающие сделки по плану.
- Для каждой позиции:
  - LONG: вход учитывается по `ask`, выход — по `bid` (исполняемая цена продажи).
  - SHORT: вход учитывается по `bid`, выход — по `ask` (исполняемая цена выкупа).
  - Ордеры LONG отправляются в `account_id` LONG, ордеры SHORT — в `account_id` SHORT.
  - TP срабатывает только когда `net PnL >= 0.09%` (с учетом комиссии `0.05%`).
  - SL срабатывает по исполняемой цене при `gross PnL <= -0.2%`.
- В консоль выводится обратная связь по закрытию сделки: `qty`, валовый PnL, комиссия `0.05%`, итоговый (net) PnL и метка `ПРИБЫЛЬ/УБЫТОК`.
- При SL по любой позиции бот закрывает ее и уходит в cooldown 120 секунд (новые входы запрещены).

## Что можно улучшить

- Добавить отдельные асинхронные таски на управление каждой открытой сделкой.
- Учитывать комиссии/гарантийное обеспечение/доступные средства точнее по тарифу.
- Добавить логирование в файл и telegram-уведомления.
